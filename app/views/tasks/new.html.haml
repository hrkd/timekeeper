%h1 New Task
= form_for @task,url: '/tasks/create', html: {method: "patch"} do |f|
  = f.text_field :name, :class => 'form-control'
  = f.submit "Create", :class => 'btn btn-default btn-primary'

= link_to 'Back', tasks_path
